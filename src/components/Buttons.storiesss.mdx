import { Meta, Story, Canvas } from "@storybook/addon-docs/blocks";
import { VoogButton } from "./Buttons";
import { action } from "@storybook/addon-actions";

<Meta
  title="Buttons"
  component={VoogButton}
  argTypes={{
    disabled: { control: "boolean" },
    isAlternate: { control: "boolean" },
  }}
/>

# Buttons

Buttons are used to trigger actions within an application.

## Action Button

<Canvas>
  <Story name="Action Button">
    <VoogButton modifiers="small">Let's do it</VoogButton>
    <br />
    <VoogButton onClick={action("button-click")}>Let's do it</VoogButton>
    <br />
    <VoogButton disabled>Let's do it</VoogButton>
  </Story>
</Canvas>

## Alternate Action Button

<Canvas>
  <Story name="Alternate Action Button">
    <VoogButton isAlternate modifiers="small">
      Let's do it
    </VoogButton>
    <br />
    <VoogButton isAlternate>Let's do it</VoogButton>
    <br />
    <VoogButton isAlternate disabled>
      Let's do it
    </VoogButton>
  </Story>
</Canvas>

## Dark Mode Buttons

<Canvas>
  <Story name="Dark Mode Button">
    <div style={{ backgroundColor: "black", padding: 15 }}>
      <VoogButton modifiers={["small", "dark"]}>Let's do it</VoogButton>
      <br />
      <VoogButton isAlternate modifiers="dark">
        Let's do it
      </VoogButton>
      <br />
      <VoogButton isAlternate disabled modifiers="dark">
        Let's do it
      </VoogButton>
    </div>
  </Story>
</Canvas>
