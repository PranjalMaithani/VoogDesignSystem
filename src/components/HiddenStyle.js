const hiddenStyle = `
border: 0;
  clip: rect(0 0 0 0);
  clippath: inset(50%);
  margin: 0px;
  overflow: hidden;
  padding: 0;
  position: absolute;
  white-space: nowrap;
  width: 1px;
  height: 1px;
`;

export default hiddenStyle;
